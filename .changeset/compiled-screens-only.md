---
'@bedrock-core/config': minor
---

**Breaking.** Every screen the config UI draws is compiled into the pack. `App`, `AppProps`, `AppRoutes` and `AppScreen` are removed from `@bedrock-core/config`.
